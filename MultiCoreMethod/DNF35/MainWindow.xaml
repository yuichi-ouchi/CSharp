<Window x:Class="DNF35.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DNF35"
        mc:Ignorable="d"
        Title=".NET 3.5" Height="400" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button x:Name="startButton" VerticalAlignment="Bottom"   Grid.Row="0" Width="120" Height="30" HorizontalAlignment="Right" Content="START" Margin="0,0,10,10" Click="startButton_Click" />
        <DockPanel Grid.Row="1" >
            <TextBox x:Name="textBox"  Margin="10"  VerticalScrollBarVisibility="Visible" />
        </DockPanel>
        
    </Grid>
</Window>
